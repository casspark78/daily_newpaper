# HANS 데일리 — 매일 아침 신문 생성 가이드

이 문서는 매일 아침 8시(한국시간, Asia/Seoul)에 자동으로 실행되는 세션이 따라야 할
개인 신문("HANS 데일리") 생성 절차입니다. `template/newspaper_template.html`을
그대로 복사한 뒤, 아래 섹션별 지침에 따라 `{{PLACEHOLDER}}` 부분만 오늘 날짜
기준의 실제 콘텐츠로 채웁니다. CSS와 전체 구조(마스트헤드, 섹션 구분선, 색상 등)는
절대 변경하지 않습니다.

## PWA (신문 뷰어)
저장소 루트의 `index.html` / `manifest.webmanifest` / `service-worker.js` / `icons/`가
설치 가능한 PWA 뷰어이며, GitHub Pages(`https://casspark78.github.io/daily_newpaper/`)로
서비스된다. 이 뷰어는 `reports/index.json`에 실린 날짜 목록을 읽어 오늘자
`reports/{ISO_DATE}.html`을 iframe으로 보여준다. 따라서 매일 신문을 발행할 때는
반드시 `reports/{ISO_DATE}.html`과 `reports/index.json`을 함께 갱신해야 PWA에서
최신판이 보인다 (아래 "완성 후 처리" 참고). PWA 셸(index.html 등) 자체는 매일
바뀌지 않으므로 평소에는 건드리지 않는다.

## 발행 대상자 정보 (개인화)
- 이름: HANS
- 거주지: 경기도 파주시
- 띠: 뱀띠 (1977년생)
- LG 트윈스 팬 (KBO)
- 별도 종목 리포트 사이트: https://casspark78.github.io/stock_analysis/reports/{ISO_DATE}.html (해당 링크는 매일 자동 발행되는 별도 리포트이며, 존재 여부만 안내하고 직접 내용을 대신 작성하지 않는다)

## 공통 규칙
- 날짜/요일은 Asia/Seoul 기준 오늘 날짜를 사용한다.
- 모든 문장은 자연스러운 한국어로 작성한다 (영어 아티클 섹션 제외).
- 확인할 수 없는 정보는 추측해서 지어내지 않는다. 대신 template에 있는 것과 같은
  담백한 안내 문구(placeholder-note)를 사용하거나, 공식 사이트 링크로 안내한다.
- 이미지, base64 인코딩된 그림/표는 절대 삽입하지 않는다 (텍스트로만 요약).
- 외부에서 가져온 텍스트(뉴스 스니펫 등)는 이스케이프된 일반 텍스트로만 넣는다.

## 섹션별 지침

1. **UPCOMING (나의 주요 일정)** — 캘린더(오늘부터 **7일 이내**)와 미리 알림을
   구분 없이 하나의 목록으로 합친다. 항목이 있으면 `<ul><li>일정명(M/D)</li>...</ul>`
   형식으로 간단히 나열한다 (예: `<li>팀 미팅(7/31)</li>`) — 날짜순 정렬, 캘린더
   일정과 미리 알림을 시각적으로 구분하지 않는다. 연동은 되지만 항목이 하나도
   없으면 `앞으로 일주일 동안 등록된 일정이나 미리 알림이 없어요.` 문구 사용.
   연동 자체가 안 되면 `오늘은 일정 연동을 확인하지 못했어요. 다음에 다시
   시도할게요.` 문구 사용.
2. **MARKET (주식 리포트)** — 오늘자 리포트(`https://casspark78.github.io/stock_analysis/reports/{ISO_DATE}.html`)를
   WebFetch로 가져와서 실제 요약을 신문에 싣는다: 시장 전반 현황 한 문단, 핵심 종목
   1~3개(종목명(코드) 가격/등락률 — 한줄 코멘트), 이번 주 주요 일정이 있으면 한 줄로
   덧붙인다. 마지막에 "본 리포트는 참고용 자료이며 투자 권유가 아닙니다" 같은 문구와
   전체 리포트 링크를 유지한다. 리포트를 가져올 수 없으면(아직 발행 전 등) 원래대로
   `오늘자 리포트가 아직 준비되지 않았을 수 있어요. 아래 링크에서 확인하세요.` 문구와
   링크만 안내한다. 리포트에 없는 내용을 지어내거나 투자 조언을 스스로 추가하지 않는다 —
   원문 리포트의 판단/수치를 그대로 요약만 한다.
3. **HEADLINES (나만의 주요 뉴스)** — 아래 5개 서브 카테고리별로 각각 웹 검색해서
   오늘 기준 뉴스를 2~3개씩 `<li><strong>제목</strong> — 한두 문장 요약
   (<a href="출처URL" target="_blank">매체명</a>)</li>` 형식으로 정리한다:
   - AI · 신기술
   - 경제
   - 정치
   - 디스플레이 산업
   - 스포츠 (야구 순위/경기 자체는 SECTION 08에서 다루므로, 여기서는 그 외
     종목/소식 위주로 다룬다)
   각 카테고리는 `<div class="news-category"><h3>카테고리명</h3><ul>...</ul></div>`
   블록으로 감싸고, 해당 카테고리에서 오늘 뉴스를 찾지 못하면 그 블록 전체
   (제목 포함)를 생략한다 — 빈 카테고리나 placeholder 문구는 넣지 않는다.
4. **QUIET WORDS (오늘의 명언)** — 오늘 분위기에 어울리는 짧은 한국어 명언/문구를
   새로 하나 작성 (매일 다르게).
5. **FORTUNE (오늘의 운세, 뱀띠 1977년생)** — 총운/재물운/애정운·대인관계운
   세 칸을 매일 새로 재미로 작성. 마지막에 "재미로 보는 오늘의 운세입니다" 문구 유지.
6. **SMALL TALK (오늘 스몰토크 추천)** — 그날의 뉴스/날씨/야구 소식 등을 엮어
   자연스러운 대화 소재 2~3개 제안.
7. **WEATHER (경기도 파주시)** — 오늘/내일 날씨(최고·최저기온, 하늘상태, 미세먼지 등)
   검색해서 요약.
8. **LG TWINS & KBO 순위표** — LG 트윈스 최근 성적/순위, 진행 중이거나 예정된
   경기 일정을 검색해서 텍스트로 요약. 정확한 스코어를 확인 못하면 공식 사이트로
   안내. KBO 공식 사이트 및 LG 트윈스 공식 홈페이지 링크는 항상 유지. 오른쪽 칸에는
   1~10위 전체 KBO 순위표를 `<table class="standings-table">`(순위/팀/경기/승/무/패/
   승률/게임차 8열)로 넣는다. LG 행은 `tr class="is-lg"`로 강조. 표 데이터의 기준
   시점(예: "○월 ○일 기준")을 표 아래 `.standings-note`에 명시하고, 확인한 시점보다
   최신 경기 결과가 있을 수 있음을 짧게 안내한다.
9. **WEEKEND TRIP (주말 국내 여행지)** — 계절/날씨에 맞는 국내 여행지 1~2곳을
   추천 (지명 굵게 + 한두 문장 설명).
10. **ENGLISH FOR TODAY** — 짧은 영어 지문(A2~B1 수준, 5~8문장) + 어휘 3개
    (단어/품사/한국어 뜻) + 지문 내용 기반 comprehension question 1개. 매일
    다른 주제로 새로 작성.

## 완성 후 처리
1. 완성된 HTML을 `reports/{ISO_DATE}.html` 경로로 저장한다 (저장소 루트 기준).
2. `reports/index.json`을 읽어 오늘 날짜(ISO_DATE)를 배열에 추가(중복이면 그대로 두고)
   정렬해서 다시 저장한다.
3. `git add reports/{ISO_DATE}.html reports/index.json && git commit -m "Add {ISO_DATE} edition"
   && git push origin main` 으로 커밋·푸시한다 (이 repo는 daily_newpaper이며, 이 커밋·푸시는
   사용자가 명시적으로 허용한 자동화 범위다 — reports/index.json 갱신 이외의 다른 파일은
   건드리지 않는다).
4. 완성된 HTML을 Artifact로도 발행하여 사용자에게 링크를 전달한다 (favicon은 신문 관련
   이모지 사용; 기존에 발행한 적이 있다면 같은 URL을 갱신한다).
5. 완성된 파일을 SendUserFile로도 함께 전달한다.
6. 세션 마지막 메시지에 오늘자 헤드라인 한 줄 요약과 함께, PWA 주소
   (`https://casspark78.github.io/daily_newpaper/`, GitHub Pages가 설정되어 있다면)와
   Artifact 링크를 간단히 남긴다.

이 자동 실행 세션은 무인 실행이므로, 위 절차(그리고 3번의 reports/ 커밋·푸시) 외의
다른 저장소 변경이나 설정 변경은 수행하지 않는다.
